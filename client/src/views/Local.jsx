import React, { useState } from 'react';

const Local = (props) => {
    return (
        <div>
            <h1>
                Local Posts
            </h1>
        </div>
    );
}

export default Local;